scrap.txt
scrap.md
TODO.md
verify.md

# transient build artifact (build.sh writes it into src/)
.build-entry.css

# all build output (web → release/web, desktop → release/<platform>)
node_modules/
release/
